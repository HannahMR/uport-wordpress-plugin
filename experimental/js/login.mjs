import request from 'ajax-request'
/**
* so we need to create the topic and include the chasquiUrl
* then poll for the response (DID-JWT)
* then, decode the JWT, login into WP (if applicable)
* then delete the topic on tthe chasqui Server
*/


document.onload = function() {
  // check to see if we are on login pages
  if document.body.classList.contains('login') &&
     document.body.classList.contains('wp-core-ui') &&
     document.body.id = 'login'  {
       var qrWidth = 201, //this fits, probably 201px


}
